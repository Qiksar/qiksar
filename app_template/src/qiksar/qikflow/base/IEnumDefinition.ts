/**
 * Define an enumeration entity type, which is typically a type of information used in dropdowns and applied as metadata to other entities
 *
 * @interface
 */
export default interface IEnumDefinition {
  label?: string;
  name: string;
  icon: string;
}
